
import React from 'react';

/**
 * @deprecated This component is no longer used in the simplified authentication flow.
 * The system now uses direct Matric No + Password (Surname) verification.
 */
const SecurityPin: React.FC = () => {
  return null;
};

export default SecurityPin;
